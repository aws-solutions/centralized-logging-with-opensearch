#foreach( $eksClusterLogSource in $ctx.result.eksClusterLogSourceList )
    #foreach( $param in $eksClusterLogSource.tags )
        #set($param.key = $util.urlEncode($param.key))    
        #set($param.value = $util.urlEncode($param.value))
    #end    

#end

$util.toJson($ctx.result)

